<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.app.frimline">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Common.FRIMLINE"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Frimline"
        android:usesCleartextTraffic="true"
        tools:targetApi="n">
        <activity android:name=".MyCartActivity"></activity>
        <activity android:name=".ProductDetailActivity" />
        <activity android:name=".screens.orders.OrderHistoryViewActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.Frimline.NoActionBar" />
        <activity android:name=".AddressesActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".CategoryLandingActivityDemo" />
        <activity android:name=".screens.cart.CheckoutAddressActivity" />
        <activity android:name=".screens.cart.BillingAddressActivity" />
        <activity android:name=".CategoryHomeActivity" />
        <activity android:name=".CategoryRootActivity" />
        <activity android:name=".CategoryLandingActivity" />
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>